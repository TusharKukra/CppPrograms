#include<bits/stdc++.h>
using namespace std;

// Submatrix Sum Query on a Matrix

// you have to find Sum of a Submatrix which has { (tli, tlj) , (bri,brj) }

// Method -1 (Brute Force)
// in each query traverse each element and add them
// TC: O(Q * m * n)

// Previous Question was on Prefix Sum Matrix.
